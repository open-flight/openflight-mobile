import { PlaceholderScreen } from '../../components/PlaceholderScreen';

// Phase 2: device status (radar/trigger health, battery) + graceful shutdown.
export default function DeviceScreen() {
  return <PlaceholderScreen title="Device" subtitle="Device status and controls are coming in a later update." />;
}
