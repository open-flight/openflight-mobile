import { PlaceholderScreen } from '../../components/PlaceholderScreen';

// Phase 1: shot history list + delete.
export default function ShotsScreen() {
  return <PlaceholderScreen title="Shots" subtitle="Shot history is coming in a later update." />;
}
