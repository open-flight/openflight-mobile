import { PlaceholderScreen } from '../../components/PlaceholderScreen';

// Phase 1: session stats + clear.
export default function StatsScreen() {
  return <PlaceholderScreen title="Stats" subtitle="Session stats are coming in a later update." />;
}
